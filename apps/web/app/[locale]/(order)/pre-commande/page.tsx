import { Fragment } from 'react';

import { HeaderLema } from '@/components/pre-commande';

export default function PreCommandePage() {
    return (
        <Fragment>
            <HeaderLema />
        </Fragment>
    );
}